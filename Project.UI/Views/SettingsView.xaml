<UserControl x:Class="Project.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Border Style="{StaticResource SurfaceBorderStyle}" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="18" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="16" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="16" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="Template Settings" Style="{StaticResource H1TextStyle}" />
                <TextBlock Grid.Row="2"
                           Text="Use this page as the baseline for application-level preferences and persisted options."
                           Style="{StaticResource HintTextStyle}" />

                <Grid Grid.Row="4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="Application title" Style="{StaticResource LabelTextStyle}" />
                        <TextBox Text="{Binding AppTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Accent color" Style="{StaticResource LabelTextStyle}" />
                        <TextBox Text="{Binding PreferredAccentColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </Grid>

                <CheckBox Grid.Row="6"
                          Content="Remember window bounds"
                          IsChecked="{Binding RememberWindowBounds, Mode=TwoWay}" />

                <StackPanel Grid.Row="8" Orientation="Horizontal">
                    <Button Content="Save"
                            Width="120"
                            Command="{Binding SaveCommand}" />
                    <Button Content="Reset"
                            Width="120"
                            Margin="12,0,0,0"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding ResetCommand}" />
                    <TextBlock Text="{Binding StatusMessage}"
                               Style="{StaticResource HintTextStyle}"
                               Margin="16,0,0,0"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
