<UserControl x:Class="Project.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="{Binding Heading}" Style="{StaticResource H1TextStyle}" />
                <TextBlock Text="{Binding Summary}" Style="{StaticResource HintTextStyle}" />
            </StackPanel>

            <Button Grid.Column="1"
                    Content="Refresh"
                    Command="{Binding RefreshCommand}"
                    Width="120" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource SurfaceBorderStyle}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Total sample items" Style="{StaticResource HintTextStyle}" />
                    <TextBlock Text="{Binding TotalItems}" FontSize="36" FontWeight="SemiBold" Margin="0,8,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource SurfaceBorderStyle}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Last refresh" Style="{StaticResource HintTextStyle}" />
                    <TextBlock Text="{Binding LastUpdatedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" FontSize="24" FontWeight="SemiBold" Margin="0,8,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="4" Style="{StaticResource SurfaceBorderStyle}" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="16" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Text="Recent Items" Style="{StaticResource H2TextStyle}" />

                <ItemsControl Grid.Row="2" ItemsSource="{Binding RecentItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource SurfaceBorderStyle}" Padding="16" Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding Description}" Style="{StaticResource HintTextStyle}" Margin="0,6,0,0" />
                                        <TextBlock Text="{Binding Owner, StringFormat=Owner: {0}}" Style="{StaticResource HintTextStyle}" Margin="0,8,0,0" />
                                    </StackPanel>

                                    <Border Grid.Column="1" Style="{StaticResource BadgeBorderStyle}" VerticalAlignment="Top">
                                        <TextBlock Text="{Binding Status}" />
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>
    </Grid>
</UserControl>
